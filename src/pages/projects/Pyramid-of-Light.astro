---
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import { siteConfig } from "../../config";
import "../../styles/global.css";

// Project metadata
const projectName = "Pyramid of Light";
const projectTagline =
  "Rapid-built LED pyramid stage prop inspired by Daft Punk’s 2006 Sahara tent visuals.";
const projectSummary =
  "Designed and built in 9 days for an Egypt-themed collegiate event. Concept, geometry, custom 3D printed connectors, and full assembly under tight budget and time constraints.";

// IMPORTANT: These paths assume files live in:
// public/images/projects/Pyramid/
const ASSETS_BASE = "/images/projects/Pyramid";

const heroImg = `${ASSETS_BASE}/Pyramid_Hero.jpg`;
const imgInitialSketch = `${ASSETS_BASE}/Pyramid_InitialSketch.jpg`;
const imgCadLines = `${ASSETS_BASE}/Pyramid_CADLines.jpg`;
const imgToothpick = `${ASSETS_BASE}/Pyramid_ToothpickModel.jpg`;

const imgConnectorSketch = `${ASSETS_BASE}/Pyramid_ConnectorsPartSketch.jpg`;
const imgConnectorsPrinted = `${ASSETS_BASE}/Pyramid_ConnectorsPrinted.jpg`;
const imgConnectorsLaidOut = `${ASSETS_BASE}/Pyramid_ConnectorsLaidOut.jpg`;

const imgLightBars = `${ASSETS_BASE}/Pyramid_LightBar.jpg`;
const imgBlueTape = `${ASSETS_BASE}/Pyramid_BlueTapeTint.jpg`;

// YouTube embed (start time ~39s)
const youtubeEmbed = "https://www.youtube.com/embed/fJFgYYfPhIs?start=39";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{projectName} | {siteConfig.name}</title>
    <meta name="description" content={projectSummary} />
  </head>

  <body class="bg-background text-foreground">
    <Header />

    <main class="pt-24 pb-16">
      <section class="max-w-4xl mx-auto px-6 space-y-10">

        {/* Top header */}
        <header class="space-y-3">
          <p class="text-sm uppercase tracking-[0.25em] text-blue-500">Project</p>
          <h1 class="text-4xl sm:text-5xl font-bold text-gray-100">{projectName}</h1>
          <p class="text-base sm:text-lg text-gray-400">{projectTagline}</p>
          <p class="text-sm sm:text-base text-gray-400">{projectSummary}</p>
        </header>

        {/* Hero image (portrait-friendly) */}
        <section class="space-y-4">
          <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
            <p class="text-sm text-gray-400 mb-3">Hero</p>
            <div class="w-full flex justify-center">
              <img
                src={heroImg}
                alt="Pyramid of Light hero image"
                class="max-h-[780px] w-auto rounded-xl border border-slate-800 object-contain"
                loading="lazy"
              />
            </div>
          </div>
        </section>

        {/* Why */}
        <section class="space-y-4">
          <h2 class="text-2xl font-semibold text-gray-100">Why</h2>
          <p class="text-gray-300 leading-relaxed">
            A local DJ asked for fast decoration ideas for an Egypt-themed collegiate event.
            I pulled inspiration from Daft Punk’s 2006 Sahara tent pyramid visuals, then scoped a build
            that could be executed in under two weeks with cheap materials and minimal fabrication risk.
          </p>
          <ul class="list-disc list-inside space-y-2 text-gray-300">
            <li>Constraint: 9 days end-to-end (design, fabrication, assembly, and install).</li>
            <li>Constraint: tight budget target (under roughly $150 in parts).</li>
            <li>Constraint: stable structure for continuous operation during the event.</li>
          </ul>
        </section>

        {/* Inspiration (YouTube embed) */}
        <section class="space-y-4">
          <h2 class="text-2xl font-semibold text-gray-100">Inspiration</h2>
          <p class="text-gray-300 leading-relaxed">
            The design goal was a modular pyramid silhouette made of repeated triangular elements,
            referencing the iconic Sahara tent look.
          </p>

          <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
            <p class="text-sm text-gray-400 mb-3">Daft Punk Sahara tent reference</p>
            <div class="aspect-video w-full overflow-hidden rounded-xl border border-slate-800">
              <iframe
                class="h-full w-full"
                src={youtubeEmbed}
                title="Daft Punk Sahara tent reference clip"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                allowfullscreen
              />
            </div>
          </div>
        </section>

        {/* Concept and Geometry (portrait stack) */}
        <section class="space-y-4">
          <h2 class="text-2xl font-semibold text-gray-100">Concept and Geometry</h2>
          <p class="text-gray-300 leading-relaxed">
            I chose five-foot PVC poles and designed the pyramid as repeated triangular frames.
            The key was locking the geometry so assembly stayed predictable under time pressure.
          </p>

          <div class="grid gap-6 md:grid-cols-2">
            <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
              <p class="text-sm text-gray-400 mb-3">Initial sketch</p>
              <div class="w-full flex justify-center">
                <img
                  src={imgInitialSketch}
                  alt="Initial sketch"
                  class="max-h-[560px] w-auto rounded-xl border border-slate-800 object-contain"
                  loading="lazy"
                />
              </div>
            </div>

            <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
              <p class="text-sm text-gray-400 mb-3">CAD line layout</p>
              <div class="w-full flex justify-center">
                <img
                  src={imgCadLines}
                  alt="CAD line layout"
                  class="max-h-[560px] w-auto rounded-xl border border-slate-800 object-contain"
                  loading="lazy"
                />
              </div>
            </div>
          </div>
        </section>

        {/* Scale Model Validation (portrait) */}
        <section class="space-y-4">
          <h2 class="text-2xl font-semibold text-gray-100">Scale Model Validation</h2>
          <p class="text-gray-300 leading-relaxed">
            I printed a scaled connector set and used a toothpick model to validate angles, joint count,
            and assembly order before committing to full-size parts.
          </p>

          <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
            <p class="text-sm text-gray-400 mb-3">Toothpick scale model validation</p>
            <div class="w-full flex justify-center">
              <img
                src={imgToothpick}
                alt="Toothpick scale model validation"
                class="max-h-[620px] w-auto rounded-xl border border-slate-800 object-contain"
                loading="lazy"
              />
            </div>
          </div>
        </section>

        {/* Connectors (portrait + rotate assembly staging) */}
        <section class="space-y-4">
          <h2 class="text-2xl font-semibold text-gray-100">Connectors</h2>
          <p class="text-gray-300 leading-relaxed">
            Custom 3D printed connectors made the structure modular and fast to assemble. The connector geometry
            was iterated quickly, then produced at volume with help from friends to hit the deadline.
          </p>

          <div class="grid gap-6 md:grid-cols-2">
            <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
              <p class="text-sm text-gray-400 mb-3">Connector part sketch</p>
              <div class="w-full flex justify-center">
                <img
                  src={imgConnectorSketch}
                  alt="Connector part sketch"
                  class="max-h-[560px] w-auto rounded-xl border border-slate-800 object-contain"
                  loading="lazy"
                />
              </div>
            </div>

            <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
              <p class="text-sm text-gray-400 mb-3">Connectors printed</p>
              <div class="w-full flex justify-center">
                <img
                  src={imgConnectorsPrinted}
                  alt="Connectors printed"
                  class="max-h-[560px] w-auto rounded-xl border border-slate-800 object-contain"
                  loading="lazy"
                />
              </div>
            </div>
          </div>

          <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
            <p class="text-sm text-gray-400 mb-3">Assembly staging</p>
            <div class="w-full flex justify-center">
              <img
                src={imgConnectorsLaidOut}
                alt="Connectors laid out for assembly staging"
                class="max-h-[560px] w-auto rounded-xl border border-slate-800 object-contain rotate-90"
                loading="lazy"
              />
            </div>
            <p class="mt-3 text-xs text-gray-500">
              Note: rotated 90 degrees for correct orientation.
            </p>
          </div>
        </section>

        {/* Lighting (vertical align, no wiring section) */}
        <section class="space-y-4">
          <h2 class="text-2xl font-semibold text-gray-100">Lighting</h2>
          <p class="text-gray-300 leading-relaxed">
            Off-the-shelf shop light bars were used as the illumination source. Diffusion and tint were added
            using blue tape to reduce harshness and tune the look.
          </p>

          <div class="grid gap-6 md:grid-cols-2">
            <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
              <p class="text-sm text-gray-400 mb-3">LED light bars</p>
              <div class="w-full flex justify-center">
                <img
                  src={imgLightBars}
                  alt="LED light bars"
                  class="max-h-[560px] w-auto rounded-xl border border-slate-800 object-contain"
                  loading="lazy"
                />
              </div>
            </div>

            <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
              <p class="text-sm text-gray-400 mb-3">Blue tape diffusion and tint</p>
              <div class="w-full flex justify-center">
                <img
                  src={imgBlueTape}
                  alt="Blue tape diffusion and tint"
                  class="max-h-[560px] w-auto rounded-xl border border-slate-800 object-contain"
                  loading="lazy"
                />
              </div>
            </div>
          </div>
        </section>

        {/* Outcome */}
        <section class="space-y-4">
          <h2 class="text-2xl font-semibold text-gray-100">Outcome</h2>
          <ul class="list-disc list-inside space-y-2 text-gray-300">
            <li>Delivered a full-scale illuminated pyramid prop under extreme time pressure.</li>
            <li>Maintained stability through the full event runtime without structural failures.</li>
            <li>Proved a fast pipeline: geometry lock, scale validation, connector iteration, then rapid assembly.</li>
          </ul>
        </section>

        {/* Back link */}
        <div class="pt-4">
          <a
            href="/projects"
            class="text-sm text-blue-400 hover:text-blue-300 underline-offset-2 hover:underline"
          >
            ← Back to all projects
          </a>
        </div>
      </section>
    </main>

    <Footer />
  </body>
</html>
